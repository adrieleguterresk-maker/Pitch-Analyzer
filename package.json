{
  "name": "pitch-analyzer-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "develop": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
